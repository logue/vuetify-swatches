/**
 * Preset color palettes for vuetify-swatches
 *
 * These palettes are available as separate imports to keep the main bundle size small.
 * Import only what you need:
 *
 * @example
 * ```typescript
 * // Import basic palette (lightweight)
 * import { basicPalette } from 'vuetify-swatches/presets';
 *
 * // Import advanced palette (larger)
 * import { advancedPalette } from 'vuetify-swatches/presets';
 * ```
 */

export { basicPalette } from './basic';
export { advancedPalette } from './advanced';
